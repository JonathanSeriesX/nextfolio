# Garbage
.DS_Store

# Editing
.idea/
.vscode/
.obsidian/
.claude/
.codex-tmp/

# Building & Deploying
.next/
.eslintcache
.vercel
.netlify
next-env.d.ts

# Regenerated by `next dev` on every run — set `agentRules: false` in
# next.config.ts if you would rather it left them alone.
AGENTS.md
CLAUDE.md

# Package management
node_modules/
yarn-error.log
.pnpm-debug.log
yarn.lock
package-lock.json
.pnpm-store

# Secrets — .env.example is the one that ships
.env*
!.env.example

# TypeScript incremental build cache
tsconfig.tsbuildinfo
