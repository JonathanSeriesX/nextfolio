import type { NextConfig } from "next";

/* Deliberately empty — the site needs no build-time configuration. Add your
   own here if you start pulling remote images (`images.remotePatterns`) or
   need a dev origin allowlist for a tunnel or container hostname. */
const nextConfig: NextConfig = {};

export default nextConfig;
